import { FileformatterPipe } from './fileformatter.pipe';

describe('FileformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FileformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
